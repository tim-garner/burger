
<br>
<br>
<br>


<h1>Tasty Burgers!</h1>

<h2>Check out the burgers</h2>
<div class="container" id="left">
<div class="row">
{{#each burgers}}
{{#unless this.devoured}}
    {{/unless}}
                        {{/each}}


</div>

<div class="container" id="right">
      <ul>
                        {{#each burgers}}
                            {{#unless devoured}}
                                <span>{{this.id}} - {{this.burger_name}}</span>
                                <button type="button" class="eatburger btn btn-warning" id ="eatbutton" data-id="{{this.id}}" data-newDevoured="{{devour}}"><span class="fa fa-cutlery"></span></button>
                                <hr>
                            {{/unless}}
                        {{/each}}
                    </ul>


</div>

</div>

<h2>Eat DA Burger</h2>
<form>
<form class="create-form">
<div class="form-group">
<input type="text"  id= "bu" name="burgername">

<p>Type the burger you like!</p>
</div>

<button type="submit">Submit</button>

</form>